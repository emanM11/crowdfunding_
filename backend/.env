# Django
SECRET_KEY=dh8veWi1F3lDT6wrIUfyCxuQaoBgVtNGRJmOP5KzXnMZbsS20j
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL
DB_NAME=crowdfunding_db
DB_USER=crowdfunding_user
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# CORS - React dev server
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Frontend base URL (used to build activation / password-reset links)
FRONTEND_URL=http://localhost:5173

# Payment gateway - 'mock' = offline Stripe-test-mode-style sandbox (no keys).
# For real Stripe test-mode charges set PAYMENT_GATEWAY=stripe + sk_test_... +
# whsec_... and VITE_STRIPE_PUBLIC_KEY=pk_test_... in the frontend .env.
PAYMENT_GATEWAY=mock
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Bonus: Facebook login — leave blank to keep the feature disabled
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

# Email — Gmail SMTP by default. While DEBUG=True with an empty
# EMAIL_HOST_USER, Django falls back to the console backend (emails +
# every link print to the runserver terminal). Fill the two fields below
# with a real Gmail App Password to send actual emails.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=team43471@gmail.com
EMAIL_HOST_PASSWORD=lenhqrtkrwtdmllj
DEFAULT_FROM_EMAIL=team43471@gmail.com